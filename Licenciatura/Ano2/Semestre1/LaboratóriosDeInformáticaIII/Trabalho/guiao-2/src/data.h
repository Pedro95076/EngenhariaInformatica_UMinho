/**
  * @file Ficheiro header referente a funções auxiliares.
  */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int compareDates(char *date_ini, char *date_end, char *date);
int verifyDate(char *start_date, char *date);
int utf8_strlen(const char *s);